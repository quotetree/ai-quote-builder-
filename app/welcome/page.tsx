import LandingPageMarketing from "@/components/archive/LandingPageMarketing";

// Archived marketing homepage (pre-PLG). Kept accessible during the transition.
export const dynamic = "force-dynamic";

export default function WelcomePage() {
  return <LandingPageMarketing />;
}
