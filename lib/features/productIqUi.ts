/**
 * ProductIQ (plan-mode) chat UI gate
 *
 * Set to `false` to hide ProductIQ from the project chat mode picker while
 * leaving PlanModePanel, `/api/ai/plan`, and related libs intact.
 *
 * To restore: set `PRODUCT_IQ_UI_ENABLED` to `true` and ship.
 */
export const PRODUCT_IQ_UI_ENABLED = false;
